export enum UserRoute {
    Base = "/user",
}

export enum ScooterRoute {
    Base = "/scooter",
}

export enum RentRoute {
    Base = "/rent",
    PickUp = "/rent/pickup",
    ReturnScooter = "/rent/return",
}
